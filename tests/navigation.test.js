import TestRenderer from 'react-test-renderer';


// list view is created with detail view lins that have filename as param

// 

// when map view gets params it calls open callout with filename param

// map view creates marker refs with ids of filenames
describe("navigations integration test", () => {
  it("sends right paramson detail view map link click, filename", () => {

  });
});
